<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.0-beta1/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-giJF6kkoqNQ00vy+HMDP7azOuL0xtbfIcaT9wjKHr8RbDVddVHyTfAAsrekwKmP1" crossorigin="anonymous">
    <title>notesenter - Edit Note</title>
</head>

<body>
    <div class="container">
      <nav class="navbar navbar-dark bg-primary">
        <div class="container-fluid">
          <a class="navbar-brand" href="/"><h2>notesenter</h2></a>
          <a href="/new" class="btn btn-outline-light">New Note</a>
        </div>
      </nav>

        <form action="/notes/<%= note._id %>?_method=PUT" method="post" class="border p-3 m-2 rounded">
            <div class="mb-3">
                <label for="title" class="form-label">Title</label>
                <input type="text" class="form-control" name="title" maxlength="100" required value="<%= note.title %>">
              </div>

              <div class="mb-3">
                <label for="description" class="form-label">Note</label>
                <textarea class="form-control" name="description" rows="7" maxlength="1000" required><%= note.description %></textarea>
              </div>
              
              <div class="btn-group" role="group" aria-lable="buttons">
                  <button class="btn btn-outline-danger">Save</button>
                <a href="/" class="btn btn-outline-warning">Cancel</a>
              </div>
        </form>
    </div>
</body>

</html>